
#
set $rosewater #f2d5cf
set $flamingo #eebebe
set $pink #f4b8e4
set $mauve #ca9ee6
set $red #e78284
set $maroon #ea999c
set $peach #ef9f76
set $yellow #e5c890
set $green #a6d189
set $teal #81c8be
set $sky #99d1db
set $sapphire #85c1dc
set $blue #8caaee
set $lavender #babbf1
set $text #c6d0f5
set $subtext1 #b5bfe2
set $subtext0 #a5adce
set $overlay2 #949cbb
set $overlay1 #838ba7
set $overlay0 #737994
set $surface2 #626880
set $surface1 #51576d
set $surface0 #414559
set $base #303446
set $mantle #292c3c
set $crust #232634


# target                 title     bg    text   indicator  border
client.focused           #babbf1 #303446 #c6d0f5  #f2d5cf  #babbf1
client.focused_inactive  #737994 #303446 #c6d0f5  #f2d5cf  #737994
client.unfocused         #737994 #303446 #c6d0f5  #f2d5cf  #737994
client.urgent            #ef9f76 #303446 #ef9f76  #737994  #ef9f76
client.placeholder       #737994 #303446 #c6d0f5  #737994  #737994
client.background        #303446



focus_follows_mouse yes
mouse_warping output 
hide_edge_borders smart
default_border pixel 2 
default_floating_border pixel 2


# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:Noto Sans Arabic:style=Regular 10,MesloLGS Nerd Font Mono:style=Regular 8
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+z exec --no-startup-id alacritty
# thunar
exec_always --no-startup-id /usr/bin/thunar --daemon 
exec_always --no-startup-id /usr/bin/thunar-volman 

#bindsym $mod+w exec --no-startup-id sudo stacer

bindsym $mod+d  exec --no-startup-id rofi -show drun -show-icons -theme .config/rofi/rofi.mz/themes/sidetab-nord.rasi         
#bindsym $mod+m exec --no-startup-id ~/.config/i3/run_niced_app vlc
bindsym $mod+n exec --no-startup-id mpv --idle --force-window=yes

#bindsym $mod+z exec --no-startup-id  $HOME/.config/polybar/scripts/wallz.py
#exec_always --no-startup-id $HOME/.config/polybar/scripts/wallz.py
#exec_always --no-startup-id /home/mz/.local/bin/wal -R

# nice -n 10 ionice -c2 -n7 

#bindsym $mod+z exec --no-startup-id nice -n 10 ionice -c2 -n7 feh --bg-scale --randomize ~/backgrounds/
#bindsym $mod+z exec --no-startup-id nice -n 10 ionice -c2 -n7 feh --bg-scale ~/Pictures/antarctica-iceberg-ocean.jpg

bindsym Mod1+Tab exec --no-startup-id "rofi -show  window -show-icons \
  -theme ~/.config/rofi/rofi.mz/themes/rounded-blue-dark.rasi \
  -kb-row-right Alt+Tab \
  -kb-row-left Alt+ISO_Left_Tab \
  -kb-accept-entry '!Alt-Tab,!Alt+ISO_Left_Tab,Return' \
  -kb-cancel Escape"



#bindsym $mod+Return  exec pkill polybar
#bindsym $mod+d exec rofi -show drun -theme ~/.config/rofi/glue_pro_blue.rasi 
bindsym $mod+Shift+t exec lxappearance

exec_always --no-startup-id setxkbmap -layout us,ara
exec_always --no-startup-id setxkbmap -layout us,ara -option 'grp:alt_shift_toggle'
#exec_always --no-startup-id setxkbmap -layout us,ara -option 'grp:win_space_toggle'



#exec_always --no-startup-id nice -n 10 ionice -c2 -n7 feh --bg-scale --randomize ~/Pictures/antarctica-iceberg-ocean.jpg
exec_always --no-startup-id nice -n 10 ionice -c2 -n7 feh --bg-scale  ~/Pictures/antarctica-iceberg-ocean.jpg
#exec_always --no-startup-id nice -n 10 ionice -c2 -n7 lxpolkit
exec_always --no-startup-id nice -n 10 ionice -c2 -n7 picom --config ~/.config/picom/picom.conf
exec_always --no-startup-id nice -n 10 ionice -c2 -n7 $HOME/.config/polybar/launch.sh
exec_always --no-startup-id nice -n 10 ionice -c2 -n7 autotiling
exec_always --no-startup-id nice -n 10 ionice -c2 -n7 flameshot   
exec_always --no-startup-id .config/conky/launc.sh
exec_always --no-startup-id mpd
exec --no-startup-id /usr/libexec/xfce-polkit
#exec_always --no-startup-id  /usr/libexec/polkit-gnome-authentication-agent-1 

# Gaps between windows
gaps inner 5
gaps outer -2 
#smart gaps 
# Gaps between screen edge and windows (per side)
#gaps outer top 0
#gaps outer bottom 10
#gaps outer left 5
#gaps outer right 15


bindsym $mod+minus gaps inner current minus 1
bindsym $mod+equal gaps inner current plus 1


exec_always --no-startup-id nm-applet
exec_always --no-startup-id i3-auto-layout
bindsym  XF86MonBrightnessUp  exec --no-startup-id  brightnessctl set +5%
bindsym  XF86MonBrightnessDown  exec --no-startup-id  brightnessctl set 5%-


bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle


exec_always --no-startup-id xinput enable 12

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
#bindsym $mod+Shift+j move left
#bindsym $mod+Shift+k move down
#bindsym $mod+Shift+l move up
#bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym F11 fullscreen

bindsym F12  exec --no-startup-id ~/scripts/memreduct.sh
bindsym F2 exec --no-startup-id nice -n 10 ionice -c2 -n7 emacs
bindsym F1 exec --no-startup-id ~/scripts/powermenu.sh

bindsym Print exec --no-startup-id flameshot full -p $HOME/Pictures/screenshots/"$(date +%Y-%m-%d_%H-%M-%S).png"


# change container layout (stacked, tabbed, toggle split)

mode "window" {
    
    bindsym s layout stacking
    bindsym t layout tabbed
    bindsym n layout toggle split

    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym q mode "default"

}

bindsym $mod+w mode "window"

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# toggle between current Tab and last Tab
bindsym $mod+Tab exec i3-msg workspace back_and_forth
bindsym Control+Tab exec --no-startup-id rofi -show window -theme .config/rofi/rofi.mz/themes/rounded-blue-dark.rasi
 
# change focus between tiling / floating windows
#bindsym $mod+z focus mode_toggle

# focus the parent container
#bindsym $mod+x focus parent

# focus the child container
#bindsym $mod+x focus child

##################################################################
# i3 for_window criteria + commands
# ------------------------------------

# 1. criteria (to identify the window)
# class="AppClass"        -> the window's class name
# instance="AppInstance"  -> the window's instance name
# title="Window Title"    -> the window's title
# multiple criteria can be combined: [class="AppClass" title="Window Title"]

# 2. window commands
# floating enable          -> enable floating mode
# floating disable         -> disable floating
# move position <x> <y>    -> move window to specific coordinates
# move position center     -> move window to the screen center
# move workspace <name>    -> move window to a specific workspace
# move scratchpad          -> send window to the scratchpad
# resize set <w> <h>       -> set window size in pixels
# resize grow <amount> px  -> increase window size
# resize shrink <amount> px-> decrease window size
# border normal|pixel|none -> set window border type
# kill                     -> close the window


#xprop | grep WM_CLASS class
#xprop | grep WM_NAME title

#for_window [class="Gimp"] floating enable
for_window [class="gnome-calculator"] floating enable
for_window [title="emacs-run-launcher"] floating enable

# workspace Tweaks

#set $ws0 "1:" 
#set $ws1 "2:"
#set $ws2 "3:"
#set $ws3 "4:"
#set $ws4 "5:" 
#set $ws5 "5:" 
#set $ws6 "7:" 
#set $ws7 "8:" 
#set $ws8 "9:" 
#set $ws9 "10:" 


#for_window [class="Chromium"] move container to workspace 2, workspace 2
#for_window [class="Chromium"] move container to workspace 2
#assign [class="Chromium"] workspace 2
 

assign [class="Chromium"] workspace 2
assign [class="Alacritty"] workspace 1
assign [class="VSCode"] workspace 5
assign [class="mpd"] workspace 4
assign [class="Thunar"] workspace 3
assign [class="mpv"] workspace 4
assign [class="TelegramDesktop"] workspace 7
assign [class="Lxappearance"] workspace 10


for_window [class="Alacritty"] layout tabbed
for_window [class="Thunar"] layout tabbed

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"



# my mode for mpd

mode "music" {
   
    bindsym Return exec --no-startup-id ~/scripts/window-script.sh /usr/bin/ncmpcpp
    bindsym space exec --no-startup-id mpc toggle
    bindsym l exec --no-startup-id mpc next
    bindsym h exec --no-startup-id mpc prev
    bindsym s exec --no-startup-id mpc stop
    bindsym c exec --no-startup-id mpc repeat
    bindsym r exec --no-startup-id mpc random
    
    bindsym 0     exec --no-startup-id mpc seek 0
    bindsym Right exec --no-startup-id mpc seek +10
    bindsym Left  exec --no-startup-id mpc seek -10

    bindsym o exec --no-startup-id ~/.config/mpd/scripts/mpd_tracks.sh
    bindsym p exec --no-startup-id ~/.config/mpd/scripts/mpd_playlists.sh

    bindsym Escape mode "default"
    bindsym q mode "default"
}
bindsym $mod+m mode "music"



# resize window (you can also use the mouse for that)
mode "resize" {
        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Up resize grow height 10 px or 10 ppt
        bindsym Down resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

mode "Apps" {

        bindsym d exec --no-startup-id nice -n 10 ionice -c2 -n7 chromium --app=https://github.com/mz2012007
        bindsym a exec --no-startup-id nice -n 10 ionice -c2 -n7 chromium --app=https://chat.openai.com
        bindsym g exec --no-startup-id nice -n 10 ionice -c2 -n7 chromium --app=https://google.com
        bindsym e exec --no-startup-id nvim-qt
        bindsym c exec --no-startup-id nice -n 10 ionice -c2 -n7 chromium
        bindsym t exec --no-startup-id nice -n 10 ionice -c2 -n7 thorium-browser
        bindsym f exec --no-startup-id nice -n 10 ionice -c2 -n7 firefox
        bindsym l exec --no-startup-id sudo lightdm-gtk-greeter-settings
        bindsym v exec --no-startup-id nice -n 10 ionice -c2 -n7 code
        bindsym p exec --no-startup-id thunar
        bindsym s exec --no-startup-id ~/scripts/window-script.sh ~/scripts/services/main.sh
        bindsym m exec --no-startup-id gnome-calculator 
        bindsym f exec --no-startup-id flameshot gui

        #bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym q mode "default"
  }
bindsym $mod+a mode "Apps"

exec_always --no-startup-id /usr/bin/emacs --daemon
bindsym $mod+Return exec emacsclient -c #--debug-init
# bindsym $mod+Shift+Return exec emacsclient -cF "((visibility . nil))" -e "(emacs-counsel-launcher)" 
bindsym $mod+Shift+Return exec emacsclient -cF "((visibility . nil))" -e "(emacs-run-launcher)" 

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        status_command i3status
#}
